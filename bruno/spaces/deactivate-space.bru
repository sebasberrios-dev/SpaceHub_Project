meta {
  name: Deactivate Space
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/spaces/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("status 200 or 404", function() {
    expect([200, 404]).to.include(res.status);
  });
}
