meta {
  name: Update Space
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/spaces/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "name": "Sala A Actualizada",
    "capacity": 12
  }
}

tests {
  test("status 200 or 404", function() {
    expect([200, 404]).to.include(res.status);
  });
}
