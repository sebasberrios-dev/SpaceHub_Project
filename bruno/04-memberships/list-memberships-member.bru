meta {
  name: List Memberships As Member
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/memberships
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 403", function() {
    expect(res.status).to.equal(403);
  });
}
