meta {
  name: Update Membership Invalid Plan
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/memberships/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "plan": "gold"
  }
}

tests {
  test("status 400", function() {
    expect(res.status).to.equal(400);
  });
}
