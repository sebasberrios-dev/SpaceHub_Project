meta {
  name: Get Reservation Not Found
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/reservations/99999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 404", function() {
    expect(res.status).to.equal(404);
  });
}
