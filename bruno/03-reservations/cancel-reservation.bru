meta {
  name: Cancel Reservation
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/reservations/1/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 200 or 403 or 404", function() {
    expect([200, 403, 404]).to.include(res.status);
  });
  test("no token returns 401", function() {
    // tested in cancel-reservation-unauth.bru
  });
}
